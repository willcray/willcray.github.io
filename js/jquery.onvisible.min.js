/*! jQuery plugin onvisible 1.0.1 - MIT license - Copyright 2015 Mikhael GERBET */ ! function (a, b) {
    a.fn.listenPosition = function () {
        return this.each(function (c, d) {
            a(d).data("visibleDone") !== !0 && a(b).scroll(function () {
                var c = a(d).position().top - a(b).scrollTop() - a(b).height() / 1.5;
                0 > c && a(d).data("visibleDone", !0).trigger("visible").unbind("visible")
            }).delay(300).trigger("scroll")
        }), this
    }
}(jQuery, window);